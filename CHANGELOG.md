# 变更记录

本项目采用日历版本号（CalVer），格式为 `YYYY.MM.DD-HH.mm`。

## 未发布

### 新增

- 三步式候选准备、检测规则、检测及结果工作流。
- 延迟检测后自动继续测速，以及对当前展示或勾选结果补充测速。
- 动态结果字段、组合筛选、自定义展示规则（按选择顺序分组取前 N）和全部/当前规则/自定义导出。
- 延迟与速度徽章颜色阈值可配置，保存到本地设置。
- 远程 TXT 导入、官方 IPv4/IPv6 网段抽样和未指定端口的延迟解析。
- Go 服务端、前端状态层、表格、导出与 DOM 接线测试。
- GitHub Actions 持续集成。
- 日志页支持级别过滤（debug / info / warn / error，debug 即显示全部级别），并按级别着色。
- IP 库工具栏按「导入导出 / 显示字段 / 更多（重命名移除、清空删除）」分组，危险操作收进「更多」菜单；库表格支持分页与显示字段选择（本地持久化）。
- IP 库导入仅保留 CSV 全字段：测速结果一键导入 + 手动 CSV 文件导入（选择文件或拖拽），自动识别表头并保留国家/城市/ASN/延迟/速度等全部字段。
- IP 库导出复用测速工作台导出面板，支持内置模板下拉与模板编辑。

### 修复

- 保留显式 `0`、`false` 配置语义，避免回落默认值。
- 用户停止或达到上限时保留已完成的部分结果。
- 自动测速阶段合并完整延迟元数据并标记正确进度阶段。
- 远程导入的内网、保留地址和 DNS rebinding 防护覆盖。
- 修复 IP 库导出弹窗残留重复模板区块（重复 id 导致交互错乱）。

正式发布时把本节重命名为版本号和发布日期，并在顶部重新建立“未发布”小节。
