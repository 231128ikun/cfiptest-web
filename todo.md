# TODO

> 当前待办清单，仅列未做项。已完成项（云端同步、日志、自动化、前端与体验等）与已放弃项（规则与自动化模板、丢包率统计）不再列出。

## 待办

- [ ] Android Gradle Wrapper：提交 gradlew / gradle-wrapper（当前依赖本机 Gradle 8.10.2，构建不可复现）
- [ ] 测速限速保护：已有延迟/测速并发上限配置；补请求间隔与总速率限制，防止测速源被压垮或结果误判
- [ ] Web 访问认证：当前仅允许 localhost；非本机访问时加简单口令保护
- [ ] 版本检查与更新内容：通过固定发布源检查最新版本，在页面展示当前版本、最新版本、发布日期和更新说明，并提供发布页或安装包下载入口
- [ ] Windows 安全更新：下载新版 exe 后校验 SHA256，由独立 updater 在主程序退出后备份并替换旧文件；更新失败时恢复旧版本，避免直接执行未经校验的远程文件

## 借鉴 CFData-WEB（新增）

- [ ] 主题手动切换：浅色 / 深色 / 跟随系统，localStorage 持久化（样式已有 prefers-color-scheme 深色适配，只差手动开关与持久化）
- [ ] 断线快照回放：SSE 重连时补发断线期间错过的结果与进度（当前断点续跑只恢复“是否在运行”，不补发已出结果）
- [ ] 发布矩阵 + 校验：现有 CI 仅 ubuntu 测试（go test/vet + JS 测试），缺 Windows / Linux / macOS × amd64 / arm64 发布产物与 SHA256 校验文件
- [ ] 内嵌 CA 证书兜底：系统证书池缺失时回退内嵌 cacert.pem，降低精简系统 / 离线环境资源下载失败率

## 待决策（追加）

- [ ] 是否做 Android WebView 壳 / 移动端（CFData 提供 APK；工程量较大，本地单机用途收益待评估）