# TODO

> 本文件记录后续功能计划，按讨论顺序推进。当前仅作为待办清单，不涉及本轮发布代码。

## 总览

- 阶段一：设置页与设置数据统一
- 阶段二：规则/自动化模板
- 阶段三：轻量日志
- 阶段四：自动化执行（本地定时已完成，外部 API 触发待定）

## 一、设置页

- [ ] 确定设置入口位置：顶部“设置”入口 vs 保留在第二步内部；推荐顶部入口，打开同页设置视图
- [ ] 设计设置页布局与导航：检测规则 / 数据源 / 结果与导出 / 自动化模板 / 日志
- [ ] 统一 `data/settings.json` 数据结构，设置页与第二步/第三步共用同一状态源，避免两套配置互相覆盖
- [ ] 迁移数据源配置：测速地址、Trace、IPS 地址、位置源、ASN 源、官方 IP 段源
- [ ] 迁移结果颜色阈值：延迟/速度区间尾端输入
- [ ] 迁移导出默认值：格式、范围、模板、字段选择
- [ ] 提供“恢复推荐设置”与“保存到本地”的状态提示
- [ ] 兼容旧版 `settings.json` 字段，升级不丢配置
- [ ] 补充设置页 DOM 接线与迁移测试

## 二、规则与自动化模板

- [ ] 定义模板数据结构：名称、检测规则、颜色阈值、导出设置、候选来源
- [ ] 支持从当前设置一键保存为模板
- [ ] 支持模板列表：应用、编辑、删除、复制
- [ ] 模板校验：速度规则启用时最低速度必填、阈值区间合法、导出模板字段合法
- [ ] 预留模板 API：`GET/PUT/DELETE /api/settings/templates`
- [ ] 模板与当前设置解耦，应用模板时不污染“当前未保存”状态
- [ ] 为未来自动化任务预留模板引用字段

## 三、日志

- [x] 后端增加轻量文件日志，写入 `data/logs/app.log`
- [x] 日志级别：`error` / `info` / `debug`；默认关闭，设置页开启后记录关键事件
- [x] 前端提供日志开关，以及“查看最近日志 / 打开日志目录”入口（独立「日志」页）
- [ ] 记录启动、任务开始/结束、资源更新、配置保存、任务失败等关键事件（已含任务开始/结束/失败与定时触发；启动、资源更新、配置保存待补）
- [ ] 任务日志包含 `taskId`、阶段、耗时，便于自动化排障（已含 `taskId` 与阶段；耗时待补）
- [x] 轮转与清理：按大小轮转（约 1MB/个、保留 3 个）
- [x] 不引入额外依赖，优先使用 Go 标准库
- [x] 前端不常驻日志窗口，独立「日志」页按需查看

## 四、自动化执行（远期）

- [x] 本地定时触发：标准 5 段 Cron（分 时 日 月 周），程序运行期间按分钟检查，同一任务同分钟不重复触发
- [ ] 外部调用 API 触发（可选，待决策）
- [x] 自动化任务流程：候选来源 → 引用模板 → 检测 → 按模板导出文件（已完成：IP 库 JSONL + 订阅器 + 编排器 + API + 前端「自动化维护」页）
- [ ] 每次运行生成运行记录，并与日志按 `taskId` 关联（调试日志已含 `taskId` 与阶段；运行记录文件尚未实现）
- [ ] 失败重试与通知策略（可选）

## 待决策

- [ ] 设置入口：顶部图标还是第二步内部
- [ ] 日志保留策略：按大小还是按天数，保留多少
- [x] 自动化触发方式：本地定时（Cron）已实现；是否补充外部 API 触发待定
- [ ] 模板是否包含导出目标文件路径
- [ ] 是否需要多用户/多方案同时运行（本地单机默认不需要）

## 完成定义

- 设置页与现有页面修改同一状态源，且升级不丢配置
- 日志不阻塞正常任务，可开关、可查看、可自动清理
- 模板可被未来自动化直接引用
- 每项功能都有对应测试与发布包验证
