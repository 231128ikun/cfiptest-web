# TODO

> 本文件记录后续功能计划，按讨论顺序推进。当前仅作为待办清单，不涉及本轮发布代码。

## 总览

- 阶段一：设置页与设置数据统一（已完成）
- 阶段二：规则/自动化模板（模板管理完善中）
- 阶段三：轻量日志（已完成，持续优化）
- 阶段四：自动化执行（本地定时与并发控制、复测与软淘汰已完成；运行记录待补）
- 阶段五：前端与体验优化
- 阶段六：云端同步（远期）
- 阶段七：代码质量与精简（已完成一轮精简复查）

## 一、规则与自动化模板

- [ ] 定义自动化模板数据结构：名称、检测规则、颜色阈值、导出设置、候选来源
- [ ] 支持从当前设置一键保存为完整模板（当前仅支持保存输出模板字符串）
- [ ] 支持模板列表管理：应用、编辑、删除、复制
- [ ] 模板校验：速度规则启用时最低速度必填、阈值区间合法、导出模板字段合法

## 二、日志

- [x] 后端增加轻量文件日志，写入 `data/logs/app.log`
- [x] 日志级别：`error` / `info` / `debug`；默认关闭，设置页开启后记录关键事件
- [x] 前端提供日志开关，以及“查看最近日志 / 打开日志目录”入口（独立「日志」页）
- [ ] 记录启动、任务开始/结束、资源更新、配置保存、任务失败等关键事件（已含任务开始/结束/失败与定时触发；启动、资源更新、配置保存待补）
- [ ] 任务日志包含 `taskId`、阶段、耗时（已含 `taskId` 与阶段；任务耗时待补）
- [x] 轮转与清理：按大小轮转（约 1MB/个、保留 3 个）
- [x] 不引入额外依赖，优先使用 Go 标准库
- [x] 前端不常驻日志窗口，独立「日志」页按需查看
- [x] 日志框固定合适尺寸并适配移动端
- [ ] 优化日志内容展示（已支持按级别过滤与着色；时间/来源分组、关键词高亮、可复制待补）

## 三、自动化执行（远期）

- [x] 本地定时触发：标准 5 段 Cron（分 时 日 月 周），程序运行期间按分钟检查，同一任务同分钟不重复触发
- [ ] 外部调用 API 触发（可选，待决策）
- [x] 自动化任务流程：候选来源 → 引用模板 → 检测 → 按模板导出文件（已完成：IP 库 JSONL + 订阅器 + 编排器 + API + 前端「自动化维护」页）
- [x] 自动维护检测参数完整分层配置：延迟并发/超时/TCP 与 HTTP 探测次数、HTTP 校验超时、测速并发/时长、连续失败移除阈值（设置页全局默认 + 任务级覆盖 + 请求覆盖）
- [x] 自动维护误判防护：HTTP(trace) 校验独立超时默认 3s，连续失败达阈值（默认 3 次）才从库移除，未达阈值保留并累计
- [x] 自动维护来源复用多源导入：本地文件持久化、远程 TXT/CSV URL、Cloudflare 官方 IPv4/IPv6
- [x] Cloudflare 官方来源支持 HTTP/HTTPS 与官方端口选择
- [x] 语义明确：官方/文件/URL 仅作为「初始化来源」导入 IP 库（重复导入保留已测元数据、不重置为未测）；每次维护候选只从库内收集
- [x] 复测机制：自动维护定期对 IP 库候选重新检测并更新状态，按连续失败阈值（默认 3 次）淘汰失效条目
- [ ] 每次运行生成运行记录，并与日志按 `taskId` 关联（调试日志已含 `taskId` 与阶段；运行记录文件尚未实现）
- [ ] 失败重试与通知策略（可选）

## 四、云端同步（远期）

- [ ] 支持将检测结果 / IP 库 / 订阅输出上传到云端（未来计划）
- [ ] 云端存储与多端访问、同步策略（待设计）
- [ ] 上传鉴权与数据安全（待设计）

## 五、前端与体验优化

- [x] 统一常用按钮高度、间距和对齐，修复自动维护参数区与日志区布局
- [ ] 继续逐页视觉验收特殊控件（测速工作台 / 自动维护 / IP 库 / 设置）
- [ ] 前端整体美化（配色、间距、组件风格统一）

## 六、代码质量与精简

- [x] 完成一轮全量精简复查：合并去除冗余、抽离可复用组件/工具函数，代码量显著下降；后续按需跟进


## 七、对照参考项目（CFData-WEB）学习项

> 源码对照结论：CFData 在功能完整度、防误判与工程化上有不少可借鉴点；其代码为 GPLv3，只借鉴思路、不抄代码。

- [ ] CLI 三级配置：命令行参数 → 本地配置文件 → Web 页面，便于脚本化与批量部署
- [ ] 后台任务与断线续跑：检测在服务端后台执行，前端断开不影响任务，重连后恢复进度（当前任务不中断，但缺少断点/运行记录）
- [ ] DC 分段扫描：按数据中心/区域分段组织候选并逐段扫描，配合每段数量上限，避免单次全量扫描
- [x] 多次探测取平均：默认每个 IP 独立 TCP 探测 3 次，单次失败不判死，成功结果取平均
- [x] 失败容忍策略：支持“连续失败 N 次后淘汰”（默认 3 次，可配置），避免网络抖动误删；丢包率统计待补
- [ ] 失败复测/重试：批量测速失败条目保留并支持后续轮次复测（当前保留但无主动重试）
- [ ] 测速限速保护：批量测速带速率限制与请求间隔（CFData 约 1.2s/IP），防止测速源被压垮或结果误判
- [ ] 测速源 ISP 自动选择：按本地 ISP 自动挑选延迟最低的测速源，减少跨网测速误差
- [ ] GitHub 上传与云同步：维护产物自动上传仓库/云盘、多端拉取（并入「云端同步」阶段推进）
- [ ] Web 访问认证：非本机访问时加简单口令保护（当前仅允许 localhost）
- [ ] 版本更新检查：启动时检查新版本并提示，提供一键更新入口
- [ ] 一键重置缓存：数据/资源缓存可一键清理重建，避免脏数据影响检测
## 待决策

- [x] 设置入口：侧边栏「设置」页已实现
- [x] 日志保留策略：按大小轮转（约 1MB/个、保留 3 个）已实现
- [x] 自动化触发方式：本地定时（Cron）已实现；是否补充外部 API 触发待定
- [x] 模板输出文件路径：自动维护任务已支持输入/输出文件配置
- [ ] 是否需要多用户/多方案同时运行（本地单机默认不需要）

## 完成定义

- 设置页与现有页面修改同一状态源，且升级不丢配置
- 日志不阻塞正常任务，可开关、可查看、可自动清理
- 模板可被未来自动化直接引用
- 每项功能都有对应测试与发布包验证